fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.24
rasterio>=1.3
pyproj>=3.6
shapely>=2.0
requests>=2.31
Pillow>=10.0

# Auth: PyJWT with cryptography for asymmetric (RS256/ES256) verification
# against the identity provider's JWKS.
pyjwt[crypto]>=2.8
email-validator>=2.0

# Database. psycopg 3 with the binary wheels, so no libpq build step.
psycopg[binary,pool]>=3.1
